% Corpus: PROSE.ADHOC_COMPOUND_MODIFIER.
%
% The discriminator is frequency, and frequency is counted PER FILE. A compound
% that recurs anywhere in this fixture is non-hapax everywhere in it, so each
% recurrence case keeps its repeats inside its own block and uses a word no
% other case uses.

% @CASE adhoc-coined-once
% @EXPECT PROSE.ADHOC_COMPOUND_MODIFIER
We build a community-shift-aware sampler over the interaction graph.
% @ENDCASE

% @CASE adhoc-two-coinages-one-sentence
% @EXPECT PROSE.ADHOC_COMPOUND_MODIFIER
The brokerage-oriented encoder feeds a concatenation-based fusion network.
% @ENDCASE

% @CASE adhoc-coined-once-guided
% @EXPECT PROSE.ADHOC_COMPOUND_MODIFIER
A compass-guided traversal reaches the boundary in logarithmic time.
% @ENDCASE

% @CASE adhoc-recurring-field-term-clean
% The same compound three times: it has become a term of this document, so the
% reader's decoding cost is amortised. Frequency, not construction, is the test.
% @EXPECT none
We compare against sharding-based consensus throughout this evaluation.
Every sharding-based protocol we test assumes a synchronous network.
The sharding-based baseline remains the strongest competitor at 64 shards.
% @ENDCASE

% @CASE adhoc-recurring-blockchain-term-clean
% @EXPECT none
Existing pbft-based replication tolerates one third faulty replicas.
We adopt the pbft-based variant of the protocol without modification.
% @ENDCASE

% @CASE adhoc-allowlisted-once-clean
% @EXPECT none
The estimator is data-driven and requires no hand-tuned thresholds.
% @ENDCASE

% @CASE adhoc-allowlisted-privacy-preserving-clean
% @EXPECT none
We instantiate the protocol with a privacy-preserving aggregation layer.
% @ENDCASE

% @CASE adhoc-rewritten-as-clause-clean
% @EXPECT none
The dataset records signals that indicate what each user was shown, which lets us separate exposure from engagement.
% @ENDCASE

% @CASE adhoc-declared-named-contribution-clean
% Named explicitly, defined, and reused — this is INVENTED_CONCEPT_LABEL's path,
% and the compound is no longer a hapax.
% @EXPECT none
We refer to a mask whose density is fixed at aggregation time as a static mask.
Static masks admit the unbiased estimator, and every experiment uses static masks.
% @ENDCASE

% Syntactic and naming exemptions. The builtin flags a compound only in
% attributive position, and clears it when the author has performed an explicit
% naming act.

% @CASE adhoc-predicative-position-clean
% Predicative: the reader has already parsed the subject, so the compound is not
% decoded mid-phrase. Downgraded to silence at the mechanical layer.
% @EXPECT none
The estimator is calibration-aware, and we report both settings side by side.
% @ENDCASE

% @CASE adhoc-acronym-definition-clean
% Defining an acronym IS the explicit naming act PROSE.INVENTED_CONCEPT_LABEL
% asks for, so the mechanical layer clears it rather than passing it on.
% @EXPECT none
We adopt Latency-Budget-Aware (LBA) scheduling throughout this study.
% @ENDCASE

% @CASE adhoc-all-segments-capitalised-clean
% Every segment capitalised is a naming convention, not prose. Sentence-initial
% capitalisation raises only the first segment, so it is not swallowed here.
% @EXPECT none
The Delay-Tolerant-Enabled stack is unchanged in our deployment.
% @ENDCASE

% @CASE adhoc-multipart-left-element
% Highest-risk shape: the left element is itself a compound. Tagged, not a
% separate rule.
% @EXPECT PROSE.ADHOC_COMPOUND_MODIFIER
We use an out-of-distribution-driven sampler for the ablation study.
% @ENDCASE

% Suffix tier. `-based` is a construction, not a coinage: `X-based` is the
% compressed form of "based on X" and composes freely, so both eras use it. It is
% off by default and folded back in with LINT_ADHOC_INCLUDE_BASED=1.

% @CASE adhoc-based-suffix-off-by-default
% Awkward, but -based is not where coinage lives, and reporting it dilutes the
% signal from 16x to 4x. Silent under the default suffix set.
% @EXPECT none
We adopt a concatenation-based fusion network for the two streams.
% @ENDCASE

% @CASE adhoc-high-risk-suffix-still-fires
% @EXPECT PROSE.ADHOC_COMPOUND_MODIFIER
The pipeline uses a freshness-aware retrieval stage before reranking.
% @ENDCASE
