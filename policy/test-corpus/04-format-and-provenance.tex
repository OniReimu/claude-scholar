% Corpus: markup-level rules and the internal-provenance builtin.

% @CASE unicode-real-arrow
% @EXPECT PROSE.UNICODE_ARROWS
The reduction maps SAT → 3-COLOUR in linear time.
% @ENDCASE

% @CASE unicode-curly-quote-is-not-an-arrow
% @EXPECT none
The authors call this the “sharpness” regime, following prior notation.
% @ENDCASE

% @CASE unicode-em-dash-char-is-not-an-arrow
% @EXPECT none
The bound holds for every prefix length we tested.
% @ENDCASE

% @CASE unicode-latex-arrow-macro-clean
% @EXPECT none
The reduction maps SAT $\rightarrow$ 3-COLOUR in linear time.
% @ENDCASE

% @CASE latex-display-dollar-dollar
% @EXPECT LATEX.EQ.DISPLAY_STYLE
$$ x = \sum_i a_i b_i $$
% @ENDCASE

% @CASE latex-equation-env-clean
% @EXPECT none
\begin{equation} x = \sum_i a_i b_i \end{equation}
% @ENDCASE

% @CASE provenance-path-macro
% @EXPECT PROSE.NO_INTERNAL_PROVENANCE
The raw traces are in \path{experiments/run_17/traces.csv}.
% @ENDCASE

% @CASE provenance-column-identifier
% @EXPECT PROSE.NO_INTERNAL_PROVENANCE
We report \texttt{empirical\_rate} averaged over ten seeds.
% @ENDCASE

% @CASE provenance-superseded-version
% @EXPECT PROSE.NO_INTERNAL_PROVENANCE, PROSE.SEMICOLON_RESTRICTION
The old bound is retracted; the constant below supersedes it.
% @ENDCASE

% @CASE provenance-artifact-citation-clean
% @EXPECT none
All measurements and the harness that produced them are released with the artifact.
% @ENDCASE

% @CASE commented-out-prose-is-not-linted
% @EXPECT none
The controller reaches steady state within four rounds.
% Unfortunately an earlier draft leveraged a comprehensive tapestry of things.
% @ENDCASE

% ── PROSE.SEMICOLON_RESTRICTION ─────────────────────────────────────────────
% The builtin strips three things before judging. Each has a precision case
% here, because the failure that matters is firing on notation an author knows
% is correct — that is how a rule gets switched off.

% @CASE semicolon-joins-two-clauses
% @EXPECT PROSE.SEMICOLON_RESTRICTION
The outer codebook quantizes each block to one of 256 centroids; the inner codebook encodes the residual at 4 bits per dimension.
% @ENDCASE

% @CASE semicolon-before-however
% @EXPECT PROSE.SEMICOLON_RESTRICTION
The bound is tight in the balanced regime; however, it degrades sharply once the skew exceeds two.
% @ENDCASE

% @CASE semicolon-in-inline-math-conditioning-clean
% @EXPECT none
The estimator is defined as $p(y \mid x; \theta)$ with $\theta$ held fixed during evaluation.
% @ENDCASE

% @CASE semicolon-thin-space-macro-clean
% @EXPECT none
The measured latency is $11\;\mathrm{ms}$ at batch size 32 and $18\;\mathrm{ms}$ at batch size 64.
% @ENDCASE

% @CASE semicolon-two-plain-sentences-clean
% @EXPECT none
The outer codebook quantizes each block to one of 256 centroids. The inner codebook encodes the residual at 4 bits per dimension.
% @ENDCASE

% The tier-1 remedy (comma + conjunction) must not read as a violation. It is a
% syntactic change, not a punctuation swap: the conjunction carries the contrast.
% Splitting this one instead yields `The point-estimate rule passes. Interval
% equivalence does not.` -- 4 words plus 4 words, which is exactly the shape
% PROSE.THEATRICAL_SPLIT names as a violation and tells the author to merge back
% with but/yet/and. The two cards used to contradict each other here.
% @CASE semicolon-tier1-comma-conjunction-clean
% @EXPECT none
The point-estimate rule passes, whereas interval equivalence does not.
% @ENDCASE

% @CASE semicolon-tier1-source-form-violation
% @EXPECT PROSE.SEMICOLON_RESTRICTION
The point-estimate rule passes; interval equivalence does not.
% @ENDCASE
