% Corpus: PROSE.SELF_UNDERMINING — the newest guardrail, and the one with the
% most term-of-art collisions. Half these cases exist to prove the lookaheads hold.

% @CASE su-unfortunately
% @EXPECT PROSE.SELF_UNDERMINING
Unfortunately, the pruned model recovers only 88.4 percent of baseline accuracy.
% @ENDCASE

% @CASE su-admittedly
% @EXPECT PROSE.SELF_UNDERMINING
Admittedly, the analysis assumes a static adversary.
% @ENDCASE

% @CASE su-falls-short-verdict
% @EXPECT PROSE.SELF_UNDERMINING
On long-tail classes the detector falls short of the supervised baseline.
% @ENDCASE

% @CASE su-falls-short-information-theoretic-termofart
% @EXPECT none
The achievable rate falls short of the information-theoretic limit by 0.4 bits per channel use.
% @ENDCASE

% @CASE su-falls-short-minimax-termofart
% @EXPECT none
Every estimator in this class falls short of the minimax rate on heavy-tailed inputs.
% @ENDCASE

% @CASE su-lags-behind-verdict
% @EXPECT PROSE.SELF_UNDERMINING
Our throughput lags behind the specialised kernel on short sequences.
% @ENDCASE

% @CASE su-lags-behind-timing-termofart
% @EXPECT none
The reconstruction lags behind by two frames, which the buffer absorbs.
% @ENDCASE

% @CASE su-does-not-outperform
% @EXPECT PROSE.SELF_UNDERMINING
The proposed sampler does not outperform ancestral sampling at 50 steps.
% @ENDCASE

% @CASE su-failed-to-match
% @EXPECT PROSE.SELF_UNDERMINING
We failed to match the reported numbers under the same seed.
% @ENDCASE

% @CASE su-unable-to
% @EXPECT PROSE.SELF_UNDERMINING
We were unable to evaluate on the closed-source system.
% @ENDCASE

% @CASE su-far-from-practical
% @EXPECT PROSE.SELF_UNDERMINING
End-to-end latency is still far from practical for interactive use.
% @ENDCASE

% @CASE su-merely-matches
% @EXPECT PROSE.SELF_UNDERMINING
Our variant merely matches the distillation baseline on CIFAR.
% @ENDCASE

% @CASE su-not-competitive
% @EXPECT PROSE.SELF_UNDERMINING
The quantised model is not competitive with the half-precision reference.
% @ENDCASE

% @CASE su-performance-drop
% @EXPECT PROSE.SELF_UNDERMINING
Under distribution shift the classifier suffers a performance drop.
% @ENDCASE

% @CASE su-compliant-disclosure-anchored
% @EXPECT none
Recall is 3.1 points lower on ImageNet-LT, reported in Table 4, and the gap closes to 0.6 points once the tail classes are reweighted.
% @ENDCASE

% @CASE su-compliant-quantified-unfavourable
% @EXPECT none
Throughput is 0.71 times that of the specialised kernel at sequence length 128 and 1.4 times at length 2048.
% @ENDCASE

% @CASE su-compliant-scope-limit-stated-plainly
% @EXPECT PROSE.SEMICOLON_RESTRICTION
The evaluation covers English-only corpora; multilingual behaviour is untested.
% @ENDCASE

% @CASE su-chinese-lexical
% @EXPECT PROSE.SELF_UNDERMINING
遗憾的是，本文方法在长尾类别上仍明显落后于监督基线。
% @ENDCASE

% ── PROSE.OVER_DEFENSIVE sentence layer: disclaimer-style negative predicates ─
% The card's five positional clauses pass a well-placed single disclaimer, so
% the sentence SHAPE now has an owner. Locate-only: A/B triage and the three
% keep classes are judgment. B-class factual negations must stay silent — 17 of
% 33 negative constructions on the measured manuscript were B-class, and a rule
% that eats definitions and findings gets switched off.

% @CASE overdef-a-class-author-refusal
% @EXPECT PROSE.OVER_DEFENSIVE
We do not select an optimal policy or predict reviewer behavior.
% @ENDCASE

% @CASE overdef-a-class-report-no
% @EXPECT PROSE.OVER_DEFENSIVE
We report no significance tests or causal coefficients.
% @ENDCASE

% NEGATION_CONTRAST's neither/nor locator also fires — honestly: this IS a
% neither/nor construction, and the two cards share the positive-flip remedy.
% Both locate; judgment assigns the fix once.
% @CASE overdef-a-class-artifact-subject
% @EXPECT PROSE.OVER_DEFENSIVE, PROSE.NEGATION_CONTRAST
The procedure neither repairs semantic outputs nor assigns fallback labels.
% @ENDCASE

% Keep class 2 locates by design: the regex cannot see the because-clause is a
% checkable methodological reason; judgment keeps the sentence.
% @CASE overdef-keep-class-methodological-locates-by-design
% @EXPECT PROSE.OVER_DEFENSIVE
Because the freeze is part of the process being studied, we do not use survival models.
% @ENDCASE

% @CASE overdef-b-class-finding-clean
% @EXPECT none
Equalizing the available window does not restore the regular-year pattern.
% @ENDCASE

% (uses the spec's definition example — a neither/nor numeric result would
% cross-fire NEGATION_CONTRAST's locate layer and test the wrong card)
% @CASE overdef-b-class-definition-clean
% @EXPECT none
The construct is defined over public actions whose object cannot be recovered from reviewer text.
% @ENDCASE

% Product names are not enumerable; declared in coverage_note, checked by hand.
% @CASE overdef-product-name-not-enumerable-clean
% @EXPECT none
TRACT does not count the rating association as an expected policy benefit.
% @ENDCASE
